targets:
  $default:
    builders:
      hive:
        enabled: false
        generate_for:
          - '**/models/**.dart'
        options:
          include_if_null: false
      mockito:
        enabled: true
        options:
          include_if_null: true