targets:
  $default:
    builders:
      hive|hive:
        enabled: true
        generate_for:
          - '**/models/**.dart'
        options:
          include_if_null: false
      mockito|mockito:
        enabled: false
        options:
          include_if_null: false