class VersionUseCase {
  final _championRepository;
  final _hiveService;
  const VersionUseCase(this._championRepository, this._hiveService);

  //TODO: decide whether retrieve data from cache or from network
}
